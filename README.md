

> [!IMPORTANT]
> ⚠️ **localized text**：localized text，localized text，localized text bug localized text。localized text，**localized text，localized text！localized text，localized text，localized textAIlocalized text！**


---

## ✨ localized text

- 🎬 **AI localized text** — localized text，localized text、Location、localized text
- 🎨 **Character & localized text** — AI localized text
- 📽️ **localized text** — localized text
- 🎙️ **AI localized text** — localized text
- 🌐 **localized text** — Vietnamese / localized text，localized text

---

## 🚀 localized text

**localized text**：localized text [Docker Desktop](https://docs.docker.com/get-docker/)

### localized text：localized text（localized text）

localized text，localized text：

```bash
# download docker-compose.yml
curl -O https://raw.githubusercontent.com/tysdz/text_stoty_ai/main/docker-compose.yml

# localized text
docker compose up -d
```

> ⚠️ localized text，localized text。localized text：

```bash
docker compose down -v
docker rmi ghcr.io/tysdz/text_stoty_ai:latest
curl -O https://raw.githubusercontent.com/tysdz/text_stoty_ai/main/docker-compose.yml
docker compose up -d
```

> localized text**localized text**localized text，localized text。

### localized text：localized text + Docker localized text（localized text）

```bash
git clone https://github.com/tysdz/text_stoty_ai.git
cd text_stoty_ai
docker compose up -d
```

localized text：
```bash
git pull
docker compose down && docker compose up -d --build
```

### localized text：localized text（localized text）

```bash
git clone https://github.com/tysdz/text_stoty_ai.git
cd text_stoty_ai

# localized text（localized text npm install localized text）
cp .env.example .env
# ⚠️ edit .env，localized text AI API Key（NEXTAUTH_URL localized text http://localhost:3000，localized text）

npm install

# localized text
# localized text：docker-compose.yml localized text，.env.example localized text
mysql:13306  redis:16379  minio:19000
docker compose up mysql redis minio -d

# localized text（localized text，localized text）
npx prisma db push

# localized text
npm run dev
```

> [!WARNING]
> localized text `npx prisma db push` localized text，localized text `The table 'tasks' does not exist`。localized text。

---

localized text [http://localhost:13000](http://localhost:13000)（localized text、localized text）localized text [http://localhost:3000](http://localhost:3000)（localized text）localized text！

> localized text，localized text。

> [!TIP]
> **localized text**：HTTP localized text。localized text [Caddy](https://caddyserver.com/docs/install) localized text HTTPS：
> ```bash
> caddy run --config Caddyfile
> ```
> localized text [https://localhost:1443](https://localhost:1443)

---

## 🔧 API Config

localized text**settings**Config AI localized text API Key，localized text。

> 💡 **localized text**：localized text API，localized text（OpenAI Compatible）localized text，localized text。

---

## 📦 localized text

- **localized text**: Next.js 15 + React 19
- **localized text**: MySQL + Prisma ORM
- **localized text**: Redis + BullMQ
- **localized text**: Tailwind CSS v4
- **localized text**: NextAuth.js

---

## 🤝 localized text

localized text。localized text：

- 🐛 submit [Issue](https://github.com/tysdz/text_stoty_ai/issues) localized text Bug
- 💡 submit [Issue](https://github.com/tysdz/text_stoty_ai/issues) localized text
- 🔧 submit Pull Request localized text — localized text PR localized text，localized text，localized text PR

---

**Made with ❤️ by tysdzai**

## Star History

[![Star History Chart](https://api.star-history.com/svg?repos=tysdz/text_stoty_ai&type=date&legend=top-left)](https://www.star-history.com/#tysdz/text_stoty_ai&type=date&legend=top-left)
